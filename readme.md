# my-web

정적 사이트 프로젝트

## 로컬에서 실행

### 1. Docker Compose 실행
`docker compose up -d`

### 2. 접속
브라우저에서 `http://localhost`로 접속

### 3. 중지
`docker compose down`

## 주요 설정 파일

- `docker-compose.yml`: Docker Compose 설정
- `public/`: 정적 파일 디렉토리